import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import MusicApp from './component/Music';

export default function App() {
  return (
    <View style={{flex:1}}>
    <MusicApp/>
    </View>
  );
}

const styles = StyleSheet.create({});
